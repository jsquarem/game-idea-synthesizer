DATABASE_URL="file:./dev.db"
# Encrypts workspace AI API keys at rest. Use 32-byte hex or any passphrase (hashed to 32 bytes). Required if using Settings > Workspace AI config.
WORKSPACE_SECRETS_MASTER_KEY=
