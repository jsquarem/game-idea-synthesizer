# ── Database ───────────────────────────────────────
# SQLite — no server required, stored as a local file
DATABASE_URL="file:./dev.db"

# ── Workspace Secrets ──────────────────────────────
# Encrypts workspace AI API keys at rest.
# Required only if using Settings > Workspace AI config.
# Use a 32-byte hex string or any passphrase (hashed to 32 bytes).
# Generate one with: openssl rand -hex 32
WORKSPACE_SECRETS_MASTER_KEY=
